namespace GalaxyUML.Core.Models
{
    public enum RoleEnum { Member, Organizer, Owner }
    public enum ObjectType
    {
        Diagram,
        Text,
        Box,
        ClassBox,
        Line,
        Association,
        DirectedAssociation, 
        Aggregation,
        Composition,
        Dependency,
        Generalization,
        Realization
    }
}
